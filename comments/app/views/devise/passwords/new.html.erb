

  <div id="container"><div id="container_header">Şifre Bilgisi</div>
    <div id="page">

<%= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post }) do |f| %>
  <%= devise_error_messages! %>

  <div><%= f.label "E-Posta adresinizi giriniz:" %><br />
  <%= f.email_field :email %></div><br />

  <div><%= f.submit "Şifre bilgilerini gönder" %></div>
<% end %>
<br>
<%= render "links" %>

        </div>
          </div>